import Foundation

// =============================================================================
/// Application version, kept in sync with the VERSION file at the project root.
let appVersion = "0.0.1"
